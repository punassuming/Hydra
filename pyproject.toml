[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hydra-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["redis==5.0.8"]

[tool.setuptools.packages.find]
include = ["worker*"]
